name: Backend CI/CD

on:
  push:
    branches: [ main ]
    paths: [ "backend/**" ]

env:
  AWS_REGION: ap-south-1
  ECR_REPOSITORY: cicd-demo-backend
  ECS_CLUSTER: cicd-demo-cluster
  ECS_SERVICE: cicd-demo-task-service-ryns0yn1

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://nexcod.narad.io

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and Push Docker Image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          IMAGE_URI=$ECR_REGISTRY/${{ env.ECR_REPOSITORY }}:$IMAGE_TAG
          echo "🛠️ Building image: $IMAGE_URI"
          docker build --no-cache -t $IMAGE_URI ./backend   # 👈 force rebuild, correct folder
          docker push $IMAGE_URI
          echo "IMAGE_URI=$IMAGE_URI" >> $GITHUB_ENV

      - name: Register new ECS task definition revision
        run: |
          echo "🧩 Using image URI: $IMAGE_URI"
          if [ -z "$IMAGE_URI" ]; then
            echo "❌ IMAGE_URI is empty — stopping"
            exit 1
          fi

          aws ecs describe-task-definition --task-definition cicd-demo-task \
            --query 'taskDefinition' > task-def.json

          cat task-def.json | jq --arg IMAGE "$IMAGE_URI" '
            .containerDefinitions[0].image = $IMAGE
            | {
                family: .family,
                networkMode: .networkMode,
                containerDefinitions: .containerDefinitions,
                requiresCompatibilities: .requiresCompatibilities,
                cpu: .cpu,
                memory: .memory,
                executionRoleArn: .executionRoleArn
              }
          ' > new-task-def.json

          echo "✅ Generated new task definition with image:"
          cat new-task-def.json | grep image

          aws ecs register-task-definition --cli-input-json file://new-task-def.json

      - name: Update ECS Service
        run: |
          echo "🚀 Updating ECS service..."
          NEW_TASK_DEF_ARN=$(aws ecs describe-task-definition --task-definition cicd-demo-task --query 'taskDefinition.taskDefinitionArn' --output text)
              echo "✅ Latest Task Definition ARN: $NEW_TASK_DEF_ARN"
          
          aws ecs update-service \
            --cluster ${{ env.ECS_CLUSTER }} \
            --service ${{ env.ECS_SERVICE }} \
            --task-definition $NEW_TASK_DEF_ARN \
            --force-new-deployment
